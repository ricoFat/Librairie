import Livre from "./livre.js";
export default class Panier
{
    constructor(el)
    {
        this._el = el;
        this.sessionStorage = window.sessionStorage;

        console.log(this_el);
    }
}